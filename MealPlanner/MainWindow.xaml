<Window x:Class="MealPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:MealPlanner.Views"
        mc:Ignorable="d"
        Title="Meal Planner" Height="600" Width="1000"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <TabControl materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                    Style="{StaticResource MaterialDesignFilledTabControl}">
            <TabItem Header="Planificación">
                <Grid>
                    <views:PlanificacionView />
                </Grid>
            </TabItem>
            <TabItem Header="Comidas y Recetas">
                <Grid>
                    <views:ComidasView />
                </Grid>
            </TabItem>
            <TabItem Header="Ingredientes">
                <Grid>
                    <views:IngredientesView />
                </Grid>
            </TabItem>
            <TabItem Header="Lista de Compras">
                <Grid>
                    <views:ListaComprasView />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
