<UserControl x:Class="MealPlanner.Views.RecetasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTituloReceta" Text="Seleccione una comida para editar su receta" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ComboBox x:Name="cmbComidas" DisplayMemberPath="Nombre" materialDesign:HintAssist.Hint="Comida" Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}" SelectionChanged="cmbComidas_SelectionChanged" />

            <StackPanel Orientation="Horizontal" IsEnabled="{Binding ElementName=cmbComidas, Path=SelectedItem, Converter={x:Null}}">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedItem, ElementName=cmbComidas}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <ComboBox x:Name="cmbIngredientes" DisplayMemberPath="Nombre" materialDesign:HintAssist.Hint="Ingrediente" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                <TextBox x:Name="txtCantidad" materialDesign:HintAssist.Hint="Cantidad" Width="80" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <Button x:Name="btnAgregarIngredienteReceta" Content="AGREGAR" Click="btnAgregarIngredienteReceta_Click" VerticalAlignment="Bottom" />
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dgReceta" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ingrediente" Binding="{Binding Ingrediente.Nombre}" Width="*" />
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="*" />
                <DataGridTextColumn Header="Unidad" Binding="{Binding Ingrediente.UnidadMedida}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>