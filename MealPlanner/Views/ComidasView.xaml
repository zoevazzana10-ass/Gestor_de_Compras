<UserControl x:Class="MealPlanner.Views.ComidasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Comidas" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox x:Name="txtNombreComida" materialDesign:HintAssist.Hint="Nombre de la comida" Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <ComboBox x:Name="cmbCategoriaComida" materialDesign:HintAssist.Hint="Categoría" IsEditable="True" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
            <ComboBox x:Name="cmbTemperatura" materialDesign:HintAssist.Hint="Temp." Width="100" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                <ComboBoxItem Content="Caliente" IsSelected="True"/>
                <ComboBoxItem Content="Frio"/>
            </ComboBox>
            <Button x:Name="btnAgregarComida" Content="AGREGAR" Click="btnAgregarComida_Click" VerticalAlignment="Bottom" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dgComidas" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*" />
                <DataGridTextColumn Header="Temperatura" Binding="{Binding Temperatura}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>