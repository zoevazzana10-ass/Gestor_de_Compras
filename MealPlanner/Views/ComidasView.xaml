<UserControl x:Class="MealPlanner.Views.ComidasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Sección Comidas -->
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Comidas" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBox x:Name="txtNombreComida" materialDesign:HintAssist.Hint="Nombre de la comida" Width="120" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <TextBox x:Name="txtCategoriaComida" materialDesign:HintAssist.Hint="Categoría" Width="100" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <ComboBox x:Name="cmbTemperatura" materialDesign:HintAssist.Hint="Temp." Width="80" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                    <ComboBoxItem Content="Caliente" IsSelected="True"/>
                    <ComboBoxItem Content="Frio"/>
                </ComboBox>
                <Button x:Name="btnAgregarComida" Content="AGREGAR" Click="btnAgregarComida_Click" VerticalAlignment="Bottom" />
            </StackPanel>

            <DataGrid Grid.Row="2" x:Name="dgComidas" AutoGenerateColumns="False" CanUserAddRows="False" SelectionChanged="dgComidas_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                    <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*" />
                    <DataGridTextColumn Header="Temperatura" Binding="{Binding Temperatura}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Sección Receta (Ingredientes de la comida seleccionada) -->
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTituloReceta" Text="Receta (Seleccione comida)" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16" IsEnabled="{Binding ElementName=dgComidas, Path=SelectedItem, Converter={x:Null}}">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedItem, ElementName=dgComidas}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <ComboBox x:Name="cmbIngredientes" DisplayMemberPath="Nombre" materialDesign:HintAssist.Hint="Ingrediente" Width="150" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                <TextBox x:Name="txtCantidad" materialDesign:HintAssist.Hint="Cantidad" Width="80" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                <Button x:Name="btnAgregarIngredienteReceta" Content="AGREGAR" Click="btnAgregarIngredienteReceta_Click" VerticalAlignment="Bottom" />
            </StackPanel>

            <DataGrid Grid.Row="2" x:Name="dgReceta" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ingrediente" Binding="{Binding Ingrediente.Nombre}" Width="*" />
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="*" />
                    <DataGridTextColumn Header="Unidad" Binding="{Binding Ingrediente.UnidadMedida}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
