<UserControl x:Class="MealPlanner.Views.IngredientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MealPlanner.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox x:Name="txtNombre" materialDesign:HintAssist.Hint="Nombre del ingrediente" Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox x:Name="txtCategoria" materialDesign:HintAssist.Hint="Categoría" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox x:Name="txtDonde" materialDesign:HintAssist.Hint="Dónde se consigue" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <TextBox x:Name="txtUnidad" materialDesign:HintAssist.Hint="Unidad de medida" Width="120" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <Button x:Name="btnAgregar" Content="AGREGAR" Click="btnAgregar_Click" VerticalAlignment="Bottom" />
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dgIngredientes" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*" />
                <DataGridTextColumn Header="Dónde" Binding="{Binding DondeSeConsigue}" Width="*" />
                <DataGridTextColumn Header="Unidad" Binding="{Binding UnidadMedida}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
