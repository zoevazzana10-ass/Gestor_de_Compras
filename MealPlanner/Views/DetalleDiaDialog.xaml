<UserControl x:Class="MealPlanner.Views.DetalleDiaDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Width="500" Height="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="txtTitulo" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
            <Button Content="+ AGREGAR COMIDA" DockPanel.Dock="Right" HorizontalAlignment="Right" Click="btnAgregar_Click" />
        </DockPanel>

        <DataGrid Grid.Row="1" x:Name="dgComidasDia" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tiempo" Binding="{Binding TiempoComida}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="Comida" Binding="{Binding Comida.Nombre}" Width="2*" IsReadOnly="True" />
                <DataGridTemplateColumn Header="Acción" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Eliminar" Click="btnEliminar_Click" Width="30" Height="30">
                                <materialDesign:PackIcon Kind="Delete" Foreground="Red" />
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CERRAR" Style="{StaticResource MaterialDesignFlatButton}" Click="btnCerrar_Click" />
        </StackPanel>
    </Grid>
</UserControl>