<UserControl x:Class="MealPlanner.Views.ListaComprasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <DatePicker x:Name="dpDesde" materialDesign:HintAssist.Hint="Desde" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <DatePicker x:Name="dpHasta" materialDesign:HintAssist.Hint="Hasta" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <Button x:Name="btnGenerar" Content="GENERAR LISTA" Click="btnGenerar_Click" VerticalAlignment="Bottom" />
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dgListaCompras" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ingrediente" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Cantidad Total" Binding="{Binding CantidadTotal}" Width="*" />
                <DataGridTextColumn Header="Unidad" Binding="{Binding UnidadMedida}" Width="*" />
                <DataGridTextColumn Header="Dónde se consigue" Binding="{Binding DondeSeConsigue}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
