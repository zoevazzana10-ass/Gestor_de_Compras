<UserControl x:Class="MealPlanner.Views.PlanificacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <DatePicker x:Name="dpFecha" materialDesign:HintAssist.Hint="Fecha" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            <ComboBox x:Name="cmbTiempo" materialDesign:HintAssist.Hint="Tiempo de Comida" Width="150" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                <ComboBoxItem Content="Desayuno" IsSelected="True" />
                <ComboBoxItem Content="Almuerzo" />
                <ComboBoxItem Content="Merienda" />
                <ComboBoxItem Content="Cena" />
            </ComboBox>
            <ComboBox x:Name="cmbComidas" DisplayMemberPath="Nombre" materialDesign:HintAssist.Hint="Comida a asignar" Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
            <Button x:Name="btnAgregarPlan" Content="ASIGNAR" Click="btnAgregarPlan_Click" VerticalAlignment="Bottom" />
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dgPlanificacion" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=d}" Width="*" />
                <DataGridTextColumn Header="Tiempo" Binding="{Binding TiempoComida}" Width="*" />
                <DataGridTextColumn Header="Comida" Binding="{Binding Comida.Nombre}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
